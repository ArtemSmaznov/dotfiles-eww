(defwidget config-heading [?title ?onclose]
           (box :class "config heading"
                :orientation "horizontal"
                :space-evenly false
                :spacing 15

                (label :class "title"
                       :hexpand true
                       :text title)

                (x-button :onclick onclose)))
